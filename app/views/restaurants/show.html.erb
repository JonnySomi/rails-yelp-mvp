<h1><%= @restaurant.name %></h1>
<div class="list-group">
  <h2 class="list-group-item"><%= @restaurant.category.capitalize %></h2>
  <h3 class="list-group-item"><%= @restaurant.address %></h3>
  <h3 class="list-group-item"><%= @restaurant.phone_number %></h3>
  <ul class="list-group-item"><% @restaurant.reviews.each do |review| %>
      <li class="list-group-item">
      <%= review.content %>
      </li>
      <li class="list-group-item">
      <%= "🟢" * review.rating %>
      </li>
    <% end %>
  </ul>
</div>
  <%= link_to 'Back to restaurants', restaurants_path, class: "btn btn-secondary" %>
  <%= link_to 'Add a review', new_restaurant_review_path(@restaurant), class: "btn btn-primary" %>
